#include "main.h"
/**
 * _abs - this finction print alphabet in lowercase
 *
 * @int: character input
 * Return: this will not return any value
 */
int _abs(int num)
{
	if (num < 0) 
		return (num * num);
	else
		return (num);
}
