#include "main.h"
/**
 * mul - this finction do the multuplicaition
 *
 * @a: first number input
 * @b: second number input
 * Return: this will not return any value
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
